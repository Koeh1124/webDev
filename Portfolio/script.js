//sets the width so it opens the side bar
function openNav() {
    document.getElementById("mySidenav").style.width = "250px";
}

// "closes" the bar by setting the width to o
function closeNav() {
    document.getElementById("mySidenav").style.width = "0px";
}